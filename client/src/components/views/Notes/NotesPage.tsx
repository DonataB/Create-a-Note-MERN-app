import React from "react";
import {
    NotesWrapper,
    ErrorMessage,
    NoteCard,
    NoteTitle,
    NoteContent,
  } from "./Notes.style";

  const NotesPage = () => {
      return (
          <NotesWrapper>

          </NotesWrapper>
      )
  }

  export default NotesPage;